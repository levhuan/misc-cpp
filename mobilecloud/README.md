Mini SaaS mobile-cloud backend simulation:
 - Broker: 
 - Data-center:
 - Mobile resources:

The processes communicates with each other using named-pipes.
Hence, they must co-exists in the same server (for now).

